spring.application.name=services

spring.profiles.active=dev

# Authentication and JWT
jwt.secret=VGhpcy1pcy1zZWNyZXQta2V5LXRlc3QxMjM0NTY3ODkwMTIzNDU2Nzg5MDEyMzQ1Ng==
jwt.expiration=3600000

# Spring Security default user (for dev/testing)
spring.security.user.name=admin
spring.security.user.password=admin123

# CORS Configuration
cors.allowed.origins=http://localhost:4200,https://account.aigreentick.com
cors.allowed.methods=*
cors.allowed.headers=*
cors.allow.credentials=true

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/service
spring.datasource.username=postgres
spring.datasource.password=root
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# MongoDB Configuration
spring.data.mongodb.uri=mongodb+srv://root:ashishmongo123@cluster0.sw3tj.mongodb.net/aigreentick_mongo

# File Upload Settings
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.enabled=true
spring.servlet.multipart.file-size-threshold=2KB

# Image Upload and Access
image.upload.path=media/images/
image.base.url=http://localhost:8080/api/image/


# Pagination
pagination.page-size=10

#GraphAl
spring.graphql.path=/graphql

#Actuator
# Expose all actuator endpoints
management.endpoints.web.exposure.include=*
management.endpoints.web.exposure.include=*
management.endpoints.web.base-path=/actuator
management.endpoint.health.show-details=always

#whatsapp configuration
whatsapp.base-url=https://graph.facebook.com
whatsapp.timeout.connect=5000
whatsapp.timeout.read=5000

timeout.analytics.read=5000
timeout.analytics.connect=5000

timeout.meta.connect=5000
timeout.meta.read=10000